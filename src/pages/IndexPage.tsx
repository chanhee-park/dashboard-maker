import { Container } from '@mui/material';

const IndexPage = () => {
  return (
    <Container maxWidth="lg" sx={{ mt: 4, mb: 4 }}>
      <h2>Index Page</h2>
    </Container>
  );
};

export default IndexPage;
